# scratch

Cursor Agent Skill: **`JIRA-EPIC-CHECK`** (Jira RHEA epic governance via MCP).

## Install

Copy the folder `JIRA-EPIC-CHECK` into your project or user skills directory, e.g. `.cursor/skills/JIRA-EPIC-CHECK/`, keeping **`JIRA-EPIC-CHECK.md`** as the skill body file (or symlink/copy to `SKILL.md` if your Cursor build only auto-loads that filename).
